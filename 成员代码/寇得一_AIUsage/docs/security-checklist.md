# 安全审查清单

## 审查信息

| 项目 | 内容 |
|------|------|
| **项目名称** | MorningModel — AI + Finance 国际晨报系统 |
| **审查日期** | 2026-06-26 |
| **审查人** | 寇得一 |
| **审查范围** | `prompt_security.py`, `llm_summarizer.py`, `config.py` |

---

## 第一层审查：对照 Prompt 检查

> 目标：要求的功能是否实际实现？安全约束是否落实在代码逻辑中而非只在注释里？

### 1. 输入消毒

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 控制字符移除 | 通过 | `_CONTROL_CHARS` 正则匹配并替换，非注释 |
| 零宽字符移除 | 通过 | `_ZERO_WIDTH` 正则匹配并替换，非注释 |
| 边界标记防伪造 | 通过 | `UNTRUSTED_PREFIX/SUFFIX` 被替换为 `_REDACTED` 变体 |
| 代码块注入防护 | 通过 | `` ``` `` 替换为 `'''` |
| 长空白序列压缩 | 通过 | `_LONG_WHITESPACE` 3个以上连续空格压缩 |
| 截断机制 | 通过 | `max_chars` 参数支持，超出部分标记 `[TRUNCATED]` |

### 2. Prompt 注入检测

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 忽略指令检测 | 通过 | 正则1：`ignore/disregard/forget` + `previous/system/instruction` |
| 角色伪造检测 | 通过 | 正则2：`system/developer prompt/message` |
| 泄露指令检测 | 通过 | 正则3：`reveal/print/dump` + `prompt/api_key/secret` |
| 输出操纵检测 | 通过 | 正则4-5：`return only json`、`set include/score` |
| 角色劫持检测 | 通过 | 正则6：`you are now ChatGPT/the system` |
| XML标签伪造检测 | 通过 | 正则7：`<system>`, `<developer>` 等标签 |
| 良性内容豁免 | 通过 | 安全研究关键词 + 单次匹配时豁免 |

### 3. LLM 调用安全集成

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 文章选择阶段注入检测 | 通过 | `select_articles_for_fetching()` 中检测标题和摘要 |
| 摘要阶段注入检测 | 通过 | `summarize_text()` 中检测标题和内容 |
| 过滤阶段注入检测 | 通过 | `filter_article()` 中检测标题和内容 |
| 安全系统提示 | 通过 | 所有 LLM 调用使用 `secure_messages()` |
| 不可信内容包裹 | 通过 | 所有外部内容使用 `wrap_untrusted()` |
| 模型输出验证 | 通过 | `validate_model_text_output()` 二次检测 |

### 4. 密钥管理

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 环境变量管理 | 通过 | `get_secret()` 从 `os.getenv()` 获取 |
| 日志脱敏 | 通过 | `_get_masked_api_key()` 仅显示前4后4位 |
| 硬编码检查 | 通过 | grep 确认无硬编码密钥 |
| 配置文件密钥检查 | 通过 | `config.toml` 不含密钥，仅含环境变量名 |

---

## 第二层审查：人工审查

> 目标：认证授权分层、最小权限、错误响应等业务逻辑

### 1. 认证授权

| 检查项 | 状态 | 说明 |
|--------|------|------|
| API 密钥分层管理 | 通过 | 不同 LLM 提供商使用独立的环境变量 |
| SMTP 凭据隔离 | 通过 | SMTP 用户名和密码使用独立环境变量 |
| 密钥获取失败处理 | 通过 | 抛出 ValueError 并打印警告，不静默失败 |

### 2. 最小权限

| 检查项 | 状态 | 说明 |
|--------|------|------|
| LLM 系统提示最小化 | 通过 | 安全提示仅包含必要的行为约束 |
| 文件访问范围 | 通过 | 仅读取 `collections/` 目录下的配置文件 |
| 网络访问范围 | 通过 | 仅访问配置的 RSS URL 和 LLM API 端点 |

### 3. 错误响应安全

| 检查项 | 状态 | 说明 |
|--------|------|------|
| 注入检测失败处理 | 通过 | 返回 `[Error: Article excluded by prompt-injection safety checks.]` |
| LLM 调用失败处理 | 通过 | 返回错误标记摘要，不暴露内部错误细节 |
| PDF 超限处理 | 通过 | 回退到文本内容或返回错误标记 |
| Token 超限处理 | 通过 | 自动截断并标记 `[TRUNCATED]` |

### 4. 数据验证

| 检查项 | 状态 | 说明 |
|--------|------|------|
| JSON 输出解析验证 | 通过 | `selected_indices` 类型和范围检查 |
| 配置文件格式验证 | 通过 | Pydantic BaseModel 强类型验证 |
| max_age 格式验证 | 通过 | 正则验证时间跨度格式 |

---

## 审查结论

**整体评价：** 项目的安全机制设计完整，覆盖了输入消毒、注入检测、边界标记、输出验证、密钥管理等关键环节。安全约束均在代码逻辑中实际实现，而非仅存在于注释中。

**发现的问题：** 无严重安全缺陷。建议关注的改进点见 `fix-report.md`。
